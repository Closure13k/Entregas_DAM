import 'package:flutter/material.dart';

class AppExamen extends StatelessWidget {
  const AppExamen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: true,
      home: Container(),
      theme: ThemeData.light(),

    );
  }
}
